# Список Management Commands для SpeakUp

## 📋 Наявні скрипти

### ✅ Вже створені:

1. **`init_contacts.py`** - Ініціалізація контактної інформації
   ```bash
   python manage.py init_contacts
   python manage.py init_contacts --force  # для перезапису
   ```
   - Створює/оновлює контактну інформацію (телефони, графік роботи)

2. **`add_testimonials.py`** - Додавання відгуків
   ```bash
   python manage.py add_testimonials
   ```
   - Створює 12 реалістичних відгуків з різними рейтингами
   - Автоматично публікує їх

3. **`update_achievements.py`** - Оновлення досягнень
   ```bash
   python manage.py update_achievements
   ```
   - Створює/оновлює 6 досягнень (200000+ студентів, 20 років досвіду тощо)

4. **`add_faq_from_site.py`** - Додавання FAQ
   ```bash
   python manage.py add_faq_from_site
   ```
   - Імпортує FAQ з сайту

---

### 5. **`add_advantages.py`** - Додавання переваг
   ```bash
   python manage.py add_advantages
   python manage.py add_advantages --force  # для перезапису
   ```
   - Створює 5 переваг SpeakUp з структурою HEADER-BODY-FOOTER
   - Додає 30 пунктів переваг (6 на кожну)
   - Підтримує UK та RU версії
   - Автоматично публікує їх

### 6. **`add_courses.py`** - Додавання курсів
   ```bash
   python manage.py add_courses
   python manage.py add_courses --force  # для перезапису
   ```
   - Створює 3 категорії: Діти, Дорослі групові, Дорослі індивідуальні
   - Створює курси в кожній категорії
   - Підтримує UK та RU версії

---

## 🆕 Скрипти, які потрібно створити:

### 1. **`add_news_articles.py`** - Створення статей блогу
   ```bash
   python manage.py add_news_articles
   ```
   **Що робить:**
   - Створює прикладні статті для блогу
   - Підтримує UK та RU версії
   - Додає SEO мета-дані
   - Встановлює дати публікації

   **Модель:** `NewsArticle`
   - `slug_uk`, `slug_ru`
   - `title_uk`, `title_ru`
   - `content_uk`, `content_ru`
   - `meta_description_uk`, `meta_description_ru`
   - `featured_image` (опціонально)
   - `published_at`
   - `is_published`

---


## 📝 Структура для нових скриптів

Всі скрипти мають бути в:
```
apps/core/management/commands/
```

**Базова структура:**
```python
from django.core.management.base import BaseCommand
from apps.core.models import ModelName

class Command(BaseCommand):
    help = 'Опис команди'

    def handle(self, *args, **options):
        # Логіка створення
        self.stdout.write(self.style.SUCCESS('Готово!'))
```

---

## 🎯 Пріоритет створення

1. **✅ ВИКОНАНО:**
   - ✅ `add_advantages.py` - переваги на головній сторінці
   - ✅ `add_courses.py` - продукти/курси для продажу
   - ✅ `add_faq_from_site.py` - оновлено з 12 питаннями для головної

2. **Середній пріоритет:**
   - `add_news_articles.py` - статті для блогу

---

## 💡 Приклади використання

```bash
# Ініціалізація всіх даних
python manage.py init_contacts
python manage.py update_achievements
python manage.py add_advantages
python manage.py add_courses
python manage.py add_testimonials
python manage.py add_news_articles
```

---

## 📋 ГОТОВІ КОМАНДИ ДЛЯ КОПІЮВАННЯ

### Скопіюйте і вставте в термінал:

```bash
# 1. Ініціалізація контактів
python manage.py init_contacts

# 2. Оновлення досягнень
python manage.py update_achievements

# 3. Додавання відгуків
python manage.py add_testimonials

# 4. Додавання FAQ (12 питань для головної + 8 додаткових)
python manage.py add_faq_from_site

# 5. Додавання переваг (5 переваг з 30 пунктами)
python manage.py add_advantages

# 6. Додавання курсів (3 категорії + курси)
python manage.py add_courses

# 7. ВСІ КОМАНДИ РАЗОМ (копіюйте весь блок)
python manage.py init_contacts && python manage.py update_achievements && python manage.py add_testimonials && python manage.py add_faq_from_site && python manage.py add_advantages && python manage.py add_courses
```

### Для перезапису (якщо потрібно):
```bash
# Перезаписати контакти
python manage.py init_contacts --force

# Перезаписати переваги
python manage.py add_advantages --force

# Перезаписати курси
python manage.py add_courses --force
```

---

## 📌 Примітки

- Всі скрипти мають перевіряти наявність даних перед створенням
- Використовувати `update_or_create` для уникнення дублікатів
- Додавати інформативні повідомлення про створення/пропуск записів
- Підтримувати UK та RU версії для всіх текстових полів

