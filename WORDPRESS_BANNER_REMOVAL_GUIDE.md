# 🔴 Інструкція: Видалення червоного банера з WordPress сайту

## 📍 Де шукати код в WordPress

### 1. **Тема WordPress (найімовірніше місце)**

#### Шлях до файлів теми:
```
/wp-content/themes/[назва-теми]/
```

#### Файли для перевірки:

**a) `functions.php`** (найімовірніше)
- Відкрийте: **Зовнішній вигляд → Редактор теми → functions.php**
- Шукайте: `fixed-holiday-alert` або текст "Увага! Це шахраї"
- Код може бути в кінці файлу

**b) `footer.php`**
- Відкрийте: **Зовнішній вигляд → Редактор теми → footer.php**
- Шукайте перед закриваючим тегом `</body>`

**c) `header.php`**
- Відкрийте: **Зовнішній вигляд → Редактор теми → header.php**
- Шукайте після `</head>` або на початку `<body>`

**d) `index.php`** (якщо використовується як основний шаблон)

### 2. **Плагіни WordPress**

Перевірте активні плагіни:
- **Плагіни → Встановлені плагіни**
- Шукайте підозрілі плагіни або плагіни з незрозумілими назвами
- Деактивуйте підозрілі плагіни по черзі

### 3. **Віджети/Кастомізатор**

- **Зовнішній вигляд → Віджети**
- Перевірте HTML-віджети в футері або інших зонах

### 4. **Інші місця**

- `wp-config.php` (рідко, але можливо)
- Користувацькі хуки через плагіни

---

## 🗑️ Як видалити код

### Варіант 1: Через адмін-панель WordPress

1. **Зовнішній вигляд → Редактор теми**
2. Виберіть файл (найімовірніше `functions.php`)
3. Знайдіть код з `fixed-holiday-alert`
4. Видаліть весь блок коду
5. Натисніть **Оновити файл**

### Варіант 2: Через FTP/SSH (безпечніше)

1. Підключіться до сервера через FTP або SSH
2. Перейдіть в `/wp-content/themes/[назва-теми]/`
3. Відкрийте файл (найімовірніше `functions.php`)
4. Знайдіть і видаліть код
5. Збережіть файл

### Що шукати в коді:

```javascript
// Шукайте цей текст:
fixed-holiday-alert

// Або цей:
Увага! Це шахраї

// Або цей код:
(function(){function b(){var c=a.offsetHeight;document.body.style.marginTop
```

---

## 🔒 Як захиститися від віддаленого доступу

### 1. **Вимкнути редагування файлів через адмін-панель**

Додайте в `wp-config.php` (на початку файлу, після `<?php`):

```php
define('DISALLOW_FILE_EDIT', true);
```

**Як це зробити:**
- Підключіться через FTP/SSH
- Відкрийте `/wp-config.php`
- Додайте рядок після `<?php`
- Збережіть

**Результат:** Ніхто не зможе редагувати файли теми/плагінів через адмін-панель.

### 2. **Обмежити доступ до файлів теми**

Встановіть правильні права доступу:

```bash
# Через SSH:
chmod 644 /wp-content/themes/[назва-теми]/*.php
chmod 755 /wp-content/themes/[назва-теми]/
```

### 3. **Перевірити користувачів WordPress**

1. **Користувачі → Всі користувачі**
2. Перевірте:
   - Невідомих користувачів
   - Користувачів з правами Адміністратора
   - Підозрілі email адреси
3. Видаліть підозрілі облікові записи
4. Змініть паролі всіх адміністраторів

### 4. **Перевірити активні сесії**

Встановіть плагін **WP Security Audit Log** або **Wordfence Security**:
- Вони покажуть хто і коли редагував файли
- Покажуть активні сесії користувачів

### 5. **Заблокувати віддалений доступ до файлів**

Додайте в `.htaccess` (в корені WordPress):

```apache
# Блокуємо прямий доступ до PHP файлів теми (крім через WordPress)
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
```

**УВАГА:** Це може зламати сайт! Краще використовувати `DISALLOW_FILE_EDIT`.

### 6. **Вимкнути виконання PHP в завантажених файлах**

Додайте в `.htaccess`:

```apache
# Блокуємо виконання PHP в папці uploads
<Directory /wp-content/uploads>
    php_flag engine off
</Directory>
```

### 7. **Встановити плагін безпеки**

Рекомендовані плагіни:
- **Wordfence Security** (безкоштовний)
- **iThemes Security**
- **Sucuri Security**

Вони автоматично:
- Блокуватимуть підозрілі запити
- Скануватимуть файли на зміни
- Логуватимуть всі дії

### 8. **Перевірити FTP/SSH доступ**

1. Змініть паролі FTP/SSH
2. Обмежте IP адреси, які можуть підключатися (якщо можливо)
3. Використовуйте SSH ключі замість паролів

### 9. **Перевірити базу даних**

Можливо код додається через базу даних:

1. Підключіться до phpMyAdmin
2. Виберіть базу даних WordPress
3. Виконайте SQL запит:

```sql
SELECT * FROM wp_options
WHERE option_value LIKE '%fixed-holiday-alert%'
   OR option_value LIKE '%шахра%';
```

Якщо знайдете - видаліть запис.

### 10. **Моніторинг змін файлів**

Встановіть плагін **WP Security Audit Log**:
- Він логує всі зміни файлів
- Показує хто і коли змінив файл
- Надсилає сповіщення на email

---

## ✅ Чек-лист після видалення

- [ ] Видалено код з файлів теми
- [ ] Додано `DISALLOW_FILE_EDIT` в `wp-config.php`
- [ ] Перевірено всіх користувачів
- [ ] Змінено паролі адміністраторів
- [ ] Встановлено плагін безпеки
- [ ] Перевірено базу даних
- [ ] Перевірено FTP/SSH доступ
- [ ] Перевірено активні плагіни
- [ ] Очищено кеш (якщо використовується)
- [ ] Перевірено сайт - банер більше не з'являється

---

## 🆘 Якщо код з'являється знову

1. **Перевірте логи сервера** - хто підключається
2. **Перевірте Wordfence/безпеку** - які IP блокувалися
3. **Перевірте базу даних** - можливо код в опціях
4. **Перевірте cron jobs** - можливо автоматичне додавання
5. **Зверніться до хостингу** - можливо компрометація сервера

---

## 📝 Додаткова інформація

**Типова структура коду, який потрібно видалити:**

```javascript
(function(){
  function b(){
    var c=a.offsetHeight;
    document.body.style.marginTop=c+"px"
  }
  var d="⚠️ Увага! Це шахраї!!!...",
      e="#e74c3c",
      f="#ffffff";
  if(!document.getElementById("fixed-holiday-alert")){
    var a=document.createElement("div");
    a.id="fixed-holiday-alert";
    // ... решта коду
  }
})();
```

**Або в PHP форматі:**

```php
add_action('wp_footer', function() {
    echo '<script>
    (function(){...код...})();
    </script>';
});
```







