# Flip-картки переваг - Інструкція з тестування

## Що було зроблено

### 1. CSS (повністю переписано)
- ✅ Snap scroll по 1 картці на всіх пристроях
- ✅ На desktop: видно 2 картки, scroll по 1
- ✅ 3D flip анімація (rotateY)
- ✅ Суцільні фони з градієнтами по кольорах
- ✅ Розміри: 300x400px (desktop), 280x380px (tablet), адаптивні (mobile)

### 2. HTML структура
- ✅ Front: фонове зображення + overlay + заголовок + підзаголовок + іконка "Детальніше"
- ✅ Back: всі 6 пунктів + footer
- ✅ `data-advantage-id="{{ advantage.order }}"` для градієнтів

### 3. JavaScript
- ✅ Flip при кліку
- ✅ Touch-friendly (розрізняє tap vs swipe)
- ✅ Keyboard navigation (Enter/Space)
- ✅ Accessibility (tabindex, role, aria-label)

## Як тестувати

### Desktop (>= 1024px)
1. Відкрийте http://127.0.0.1:8000/
2. Прокрутіть до секції "Наші переваги"
3. **Перевірте:**
   - Видно 2 картки одночасно
   - Scroll прокручує по 1 картці (snap)
   - Клік на картку робить flip
   - Повторний клік повертає назад
   - Текст на передній стороні читабельний (білий на темному фоні)
   - Зворотня сторона має кольоровий градієнт
   - Іконка "ⓘ Детальніше" видна

### Tablet (768-1023px)
1. Змініть розмір вікна браузера або використайте DevTools (F12 → Toggle device toolbar)
2. **Перевірте:**
   - Видно 1-1.2 картки (натяк на наступну)
   - Scroll працює плавно (snap)
   - Flip працює при кліку/тапі

### Mobile (<= 767px)
1. Використайте DevTools → iPhone/Android емуляцію
2. **Перевірте:**
   - Видно 1 картку на весь екран
   - Scroll snap працює чітко
   - Tap на картку робить flip
   - Swipe ліворуч/праворуч прокручує (не flip)
   - Всі тексти читабельні

## Градієнти карток

Кожна картка має свій колір на зворотній стороні:
1. **Картка 1** (order=1): Синій (#3b82f6 → #2563eb)
2. **Картка 2** (order=2): Зелений (#10b981 → #059669)
3. **Картка 3** (order=3): Фіолетовий (#8b5cf6 → #7c3aed)
4. **Картка 4** (order=4): Помаранчевий (#f59e0b → #d97706)
5. **Картка 5** (order=5): Бірюзовий (#14b8a6 → #0d9488)

## Troubleshooting

### Якщо snap не працює:
- Перевірте, що браузер підтримує `scroll-snap-type` (всі сучасні підтримують)
- Очистіть кеш браузера (Ctrl+Shift+R або Cmd+Shift+R)

### Якщо flip не працює:
1. Перевірте консоль браузера (F12 → Console)
2. Переконайтеся, що JS файл завантажується
3. Перевірте, що клас `.flipped` додається до картки

### Якщо зображення не відображаються:
- Додайте файли у `media/advantages/`
- Або залиште порожніми — буде показано градієнт за замовчуванням

### Якщо картки накладаються:
- Перевірте, що старі стилі не кешовані
- Hard refresh: Ctrl+F5 (Windows) або Cmd+Shift+R (Mac)

## Оптимізації

### Для production:
1. Додати індикатор scroll (точки/стрілки)
2. Preload перших 2-3 зображень
3. Lazy load для решти
4. Додати анімацію входу (fade-in при scroll в viewport)

### Accessibility:
- ✅ Keyboard navigation вже є
- ✅ ARIA labels додані
- ✅ Reduced motion підтримується

## Наступні кроки

1. ✅ Запустити сервер: `python3 manage.py runserver`
2. ✅ Відкрити http://127.0.0.1:8000/
3. ✅ Протестувати на різних розмірах
4. Додати зображення у `media/advantages/` (опціонально)
5. Налаштувати кольори градієнтів (якщо потрібно)

---

**Статус:** Готово до тестування! 🚀



